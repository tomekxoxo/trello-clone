overwrite: true
schema: 'http://localhost:3000/api/graphql'
generates:
  graphql/typesGen.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      contextType: 'graphql/context#Context'
  ./graphql.schema.json:
    plugins:
      - 'introspection'
