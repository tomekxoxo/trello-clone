import styled from 'styled-components';

export const StyledAddAnotherButtonWrapper = styled.div`
  background-color: ${({ theme }) => theme.colors.boardColor};
  bottom: 0;
  height: fit-content;
  padding: 2.4rem 0;
  position: sticky;
  top: 0;
`;
